from application.salary import calculate_salary as calc_sal
from application.db.people import get_employees as get_emp
from application.circle import circle
from datetime import datetime

if name == 'main':

date = datetime.now()  
formatted_date = date.strftime("%d-%m-%Y в %H:%M:%S")

calc_sal(formatted_date)
get_emp(formatted_date)

circle(2, 2, 5, .01)
